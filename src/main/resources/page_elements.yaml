# Sample page elements YAML file

LoginButton:
  id: userLogin
  cssSelector: #userLogin

UsernameField:
  id: username
  cssSelector: #username

PasswordField:
  id: password
  cssSelector: #password

SignInButton:
  id: signIn
  cssSelector: #signIn

viewMyTaskLink:
  id: viewMyTask
  cssSelector: #viewMyTask

addNewTaskLink:
  id: addNewTask

titleField:
  id: title

textField:
  id: text

usernameDropdown:
  id: username
  option: Amirtaraj

saveButton:
  id: saveButton

homeButton:
  id: home

logoutButton:
  id: logout